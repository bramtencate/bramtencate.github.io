class Smoke {
    constructor(x, y) {
        this.x = x;
        this.y = y;
        this.interval = 10;
    };

    render() {

        for (let i = 0; i < 100; i++) {
            if (i%this.interval == 0) {
                
            }
        }

    };
}